# Personal Assistant (CLI)

Система для зберігання та взаємодії з контактами й нотатками.

## Встановлення
```bash
python -m venv .venv && . .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install --upgrade pip
pip install -e .
