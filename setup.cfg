[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude = .venv,venv,build,dist

[isort]
profile = black
line_length = 100

[tool:pytest]
testpaths = test
addopts = -q

[coverage:run]
branch = True
source = src/assistant
